[package]
name = "op-diagram"
description = "Diagram IR: the graphs and sequences that the Mermaid parser and the task flow write and the renderer reads"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
